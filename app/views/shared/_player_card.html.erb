<div class="card border-secondary mb-3">
  <div class="card-header d-flex justify-content-between">
    Mme, Mr :
      <%= user.first_name %>
      <%= user.last_name %>
  </div>
  <div class="card-body text-secondary">
    <p class="card-text">
       Club : <%= user.club %>
      , licence N°: <%= user.licence %>
      , Classement : <%= user.ranking %>
      , Catégorie : <%= user.gender %>
      </p>
  </div>
  </div>
